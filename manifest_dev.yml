inherit: manifest_base.yml
host: dsnap_registration_service-dev
applications:
  env:
    - NEW_RELIC_APP_NAME: dsnap_registration_service (dev)
      NEW_RELIC_CONFIG_FILE: newrelic.ini
      NEW_RELIC_ENV: dev
      NEW_RELIC_LOG: stdout
