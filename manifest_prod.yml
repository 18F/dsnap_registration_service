inherit: manifest_base.yml
host: dsnap_registration_service
instances: 2
applications:
  env:
    - NEW_RELIC_APP_NAME: dsnap_registration_service (prod)
      NEW_RELIC_CONFIG_FILE: newrelic.ini
      NEW_RELIC_ENV: prod
      NEW_RELIC_LOG: stdout
